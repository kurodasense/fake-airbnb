import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const reducers = {};

const homeSlice = createSlice({
  name: "home",
  initialState,
  reducers,
});

export default homeSlice.reducer;
